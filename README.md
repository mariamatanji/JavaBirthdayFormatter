# JavaBirthdayFormatter
A simple Java program that demonstrates user input, date parsing, and date formatting. The app asks for a user’s name and birthdate, then outputs the birthday in a friendly, formatted format.
